# 鬼牌游戏 - 快速参考

## 文件清单

### ✅ 已创建的核心文件

#### 自动加载脚本（Autoload）
- `Scripts/Autoload/SteamManager.gd` - Steam 集成
- `Scripts/Autoload/NetworkManager.gd` - P2P 网络
- `Scripts/Autoload/GameManager.gd` - 游戏逻辑

#### 卡牌系统
- `Scripts/Cards/Card.gd` - 卡牌类
- `Scripts/Cards/Deck.gd` - 牌堆管理

#### UI 脚本
- `Scripts/UI/MainMenu.gd` - 主菜单
- `Scripts/UI/Lobby.gd` - 房间大厅
- `Scripts/UI/Game.gd` - 游戏主界面
- `Scripts/UI/CardUI.gd` - 卡牌组件

### 📋 需要你创建的文件

#### 场景文件（在 Godot 编辑器中创建）
- [ ] `Scenes/MainMenu.tscn`
- [ ] `Scenes/Lobby.tscn`
- [ ] `Scenes/Game.tscn`

详细创建步骤请参考：`项目设置指南.md`

#### Steam 配置文件
- [ ] 项目根目录下的 `steam_appid.txt`（内容：`480`）

#### 资源文件（可选，美术资源）
- [ ] `Resources/Cards/` - 扑克牌图片
- [ ] `Resources/Audio/` - 音效和音乐
- [ ] `Resources/UI/` - UI 图片

---

## 核心功能速查

### Steam 管理

```gdscript
# 创建房间
SteamManager.create_lobby(4, 10)  # 4人房间，10秒质疑时间

# 加入房间
SteamManager.join_lobby(lobby_id)

# 离开房间
SteamManager.leave_lobby()

# 获取当前玩家信息
var my_id = SteamManager.steam_id
var my_name = SteamManager.player_name
```

### 网络通信

```gdscript
# 设置为房主
NetworkManager.set_as_host()

# 发送广播消息
NetworkManager.send_message_to_all({
    "type": NetworkManager.MessageType.PLAYER_READY
})

# 发送给特定玩家
NetworkManager.send_message_to(player_id, message)
```

### 游戏逻辑

```gdscript
# 初始化游戏
GameManager.initialize_game(player_list, {
    "max_players": 4,
    "challenge_time": 10
})

# 开始游戏
GameManager.start_game()

# 出牌
GameManager.player_claim_cards(player_id, "A", 2, [card1, card2])

# 质疑
GameManager.player_challenge(challenger_id)

# 跟牌
GameManager.player_follow_cards(player_id, "A", 1, [card])

# 过牌
GameManager.player_pass(player_id)
```

---

## 游戏流程

### 1. 启动游戏
```
Steam 初始化 → 显示主菜单
```

### 2. 创建/加入房间
```
玩家A: 创建房间 → 获得房间码
玩家B: 输入房间码 → 加入房间
```

### 3. 房间准备
```
玩家准备 → 房主开始游戏 → 自动发牌
```

### 4. 游戏进行
```
轮到出牌 → 选择卡牌 → 声明点数 → 出牌
其他玩家 → 质疑 / 跟牌 / 过牌
```

### 5. 质疑结果
```
翻开牌 → 验证声明
- 声明正确：质疑者拿走所有牌
- 声明错误：出牌者拿回自己的牌
```

### 6. 胜利判定
```
某玩家手牌为0 → 该玩家获胜 → 下一轮由其先手
```

---

## 信号（Signals）速查

### SteamManager 信号
```gdscript
steam_initialized(success: bool)
lobby_created(lobby_id: int)
lobby_joined(lobby_id: int)
lobby_join_failed(reason: String)
player_joined(steam_id: int, player_name: String)
player_left(steam_id: int)
```

### NetworkManager 信号
```gdscript
message_received(sender_id: int, message: Dictionary)
connection_established(peer_id: int)
connection_failed(peer_id: int)
```

### GameManager 信号
```gdscript
game_started()
cards_dealt(player_id: int, cards: Array)
turn_changed(player_id: int)
cards_claimed(player_id: int, rank: String, count: int)
player_challenged(challenger_id: int, target_id: int)
challenge_result(success: bool, cards: Array, penalty_player: int)
player_won(player_id: int)
```

---

## 下一步操作

### 立即可以做的：
1. ✅ 在 Godot 中打开项目
2. ✅ 根据 `项目设置指南.md` 创建 3 个场景文件
3. ✅ 创建 `steam_appid.txt` 文件
4. ✅ 运行项目测试 Steam 初始化

### 可选增强：
- 导入扑克牌图片资源
- 添加背景音乐和音效
- 制作出牌动画
- 美化 UI 界面
- 添加聊天功能

---

## 调试技巧

### 查看日志
```gdscript
print("调试信息: ", variable)
```

### 检查 Steam 状态
```gdscript
if SteamManager.is_steam_initialized:
    print("Steam ID: ", SteamManager.steam_id)
```

### 检查网络连接
```gdscript
print("已连接玩家: ", NetworkManager.connected_peers)
```

### 检查游戏状态
```gdscript
print("当前状态: ", GameManager.current_state)
print("当前玩家: ", GameManager.current_player_index)
```

---

## 常用命令

### Git 操作
```bash
# 提交更改
git add .
git commit -m "添加功能描述"
git push

# 查看状态
git status
```

### 导出项目
```
项目 → 导出 → 添加导出预设 → Windows Desktop
```

---

## 联系支持

- 查看完整文档：`README.md`
- 详细设置指南：`项目设置指南.md`
- Godot 官方文档：https://docs.godotengine.org/
- GodotSteam 文档：https://godotsteam.com/

**明日之歌工作室**
祝开发顺利！
